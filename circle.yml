machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t quasar .:
        timeout: 1800

test:
  override:
    - docker run quasar /bin/sh -c "/usr/local/bin/ipython nbconvert --to=html --ExecutePreprocessor.enabled=True samples/*.ipynb"

