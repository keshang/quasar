machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t quasar .:
        timeout: 1800

test:
  override:
    - docker run quasar /bin/sh -c "/usr/local/bin/jupyter nbconvert --to=html --ExecutePreprocessor.enabled=True --ExecutePreprocessor.timeout=3600 samples/*.ipynb":
        timeout: 7200
    - docker run quasar /bin/sh -c "/usr/local/bin/jupyter nbconvert --to=html --ExecutePreprocessor.enabled=True --ExecutePreprocessor.timeout=3600 samples/gas/*.ipynb":
        timeout: 7200
    - docker run quasar /bin/sh -c "/usr/local/bin/nosetests /usr/local/lib/python2.7/dist-packages/pyquasar"

deployment:
  public_dev:
    branch: master
    commands:
      - curl -H "Content-Type:application/json" --data '{"docker_tag":"latest"}' -X POST https://registry.hub.docker.com/u/quantego/quasar/trigger/c1864219-515e-4f58-9344-663ba39de31d/
